

# Erik todo

Change Event event in Drone to Optional<Event> event;

Change InZoneSchedulerResponse to isInZoneSchedulerResponse

Change isInZone to check if kinematics says we are there

Move event string parsing into Event class. And throw an exception if it fails.